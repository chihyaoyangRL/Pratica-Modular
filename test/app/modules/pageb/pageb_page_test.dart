import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:modular_ex/app/modules/pageb/pageb_page.dart';

void main() {
  testWidgets('PagebPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PagebPage(title: 'Pageb')));
    //  final titleFinder = find.text('Pageb');
    //  expect(titleFinder, findsOneWidget);
  });
}
