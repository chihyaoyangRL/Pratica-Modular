import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:modular_ex/app/modules/pagea/pagea_page.dart';

void main() {
  testWidgets('PageaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PageaPage(title: 'Pagea')));
    //  final titleFinder = find.text('Pagea');
    //  expect(titleFinder, findsOneWidget);
  });
}
